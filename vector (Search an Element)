#include <iostream>
#include <vector>
using namespace std;

int main() 
{
   vector<int>numbers;

   numbers.push_back(2);
   numbers.push_back(7);
   numbers.push_back(4);
   numbers.push_back(8);
   numbers.push_back(5);
   numbers.push_back(13);

  for (int i=0;i<numbers.size();i++) {

    if (numbers[i]==13) {

    cout<<" 13 is present "<<endl;
    }
  }
    return 0;
}
